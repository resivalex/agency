h1.page-header = Vacancy.model_name.human(count: 2).mb_chars.capitalize.to_s

= link_to t(:new_vacancy), new_vacancy_path, class: 'btn btn-primary'

= render 'shared/vacancies_table', vacancies: @vacancies
